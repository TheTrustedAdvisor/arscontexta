{
  "mcpServers": {
    "ars-contexta": {
      "command": "node",
      "args": ["dist/server.js"],
      "description": "Ars Contexta vault operations: setup, validate, graph, tree, health, search"
    }
  }
}
